@model DAL.TB_GROUP_USER

@{
    ViewBag.Title = "Editar";
}

<h2>Editar registro</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TB_GROUP_USER</legend>

        @*<div class="editor-label">
            @Html.LabelFor(model => model.ID_GROUP_USER, "TB_PRODUCT")
        </div>*@
        <div class="editor-field">
            @Html.DropDownList("ID_GROUP_USER", String.Empty)
            @Html.ValidationMessageFor(model => model.ID_GROUP_USER)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ID_USER, "TB_USER")
        </div>
        <div class="editor-field">
            @Html.DropDownList("ID_USER", String.Empty)
            @Html.ValidationMessageFor(model => model.ID_USER)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ID_GROUP, "TB_GROUP")
        </div>
        <div class="editor-field">
            @Html.DropDownList("ID_GROUP", String.Empty)
            @Html.ValidationMessageFor(model => model.ID_GROUP)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.OBS)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OBS)
            @Html.ValidationMessageFor(model => model.OBS)
        </div>

        <p>
            <input type="submit" value="Salvar" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Voltar", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
