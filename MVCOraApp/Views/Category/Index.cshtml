@model IEnumerable<DAL.TB_CATEGORY>

@{
    ViewBag.Title = "Lista";
}

<h2>Listagem dos registros</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table>
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.ID_CATEGORY)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPTION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.ID_CATEGORY)
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPTION)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.ID_CATEGORY }) |
            @Html.ActionLink("Detalhes", "Details", new { id = item.ID_CATEGORY }) |
            @Html.ActionLink("Deletar", "Delete", new { id = item.ID_CATEGORY })
        </td>
    </tr>
}

</table>
